<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chernokoz.android.verbalcounting">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Verbalcounting">
        <activity android:name=".StatisticsActivity"></activity>
        <activity android:name=".TrainingResultsActivity" />
        <activity android:name=".TrainingActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".BattleResultsActivity" />
        <activity android:name=".BattleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>